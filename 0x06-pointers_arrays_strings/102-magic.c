#include <stdio.h>
int main(void)
{
	int n;
	int a[5];
	int *p;

	a[2] = 1024;
	p = &n;
	/**
	 * write line of code
	 * remember
	 * not allowed a
	 * not allowd p
	 * only one statement
	 */
	printf("a[2] = %d\n", a[2]);
	return (0);
}
